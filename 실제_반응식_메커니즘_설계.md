# 실제 화학반응식 메커니즘 — 설계 구상 (v1)

> 사용자 지적: **"지금은 원소를 끼워넣기만 한다. 그걸 실제 화학반응식(계수·반응물→생성물·질량보존)으로 구현하고 싶다."**
> 이 문서는 **구상/설계만** (구현 전). 화학교육의 핵심인 **반응식 균형(balancing)·화학량론(stoichiometry)** 을 전투에 녹이는 방안.

---

## 1. 현재 시스템 진단
- 플레이어가 원자 코인(H·O·C…)을 슬롯에 놓음 → `atoms:{H:2,O:1}` 일치하면 **분자 1개(H₂O)** 완성 → 스킬 발사.
- 즉 **"분자 조립"** 만 있고, **"반응식"**(반응물 + 반응물 → 생성물, 계수, 질량보존)은 도감에만 텍스트로 존재.
- 교육 핵심(계수 맞추기·질량보존·화학량론)이 **게임플레이에 없음**.

## 2. 목표
- **분자 조립의 재미(촉각적)** 는 유지하되, 그 위에 **"반응식 세우기" 계층**을 얹는다.
- 플레이어가 **반응물 분자 + 계수**를 조작해 **질량보존이 맞는 반응식**을 완성해야 강한 스킬이 나가게.

---

## 3. 추천 설계 — 2계층 "반응식 조립"

### 계층 1: 분자 만들기 (원자 → 분자) — *현재 시스템 유지*
- 원자 코인을 슬롯에 놓아 **분자**(H₂, O₂, CH₄, HCl…)를 만든다.
- 완성된 분자는 **분자 인벤토리**에 쌓인다. (기존 "합성"이 여기에 해당)

### 계층 2: 반응식 세우기 (분자 → 균형 반응식) — *신규*
스킬 발사 = **반응식 완성**. UI:
```
 [계수][분자]  +  [계수][분자]   →   [계수][생성물]  +  [계수][생성물]
   [2]  H₂     +    [1]  O₂       →     [2]  H₂O
 ───────────────────  ⚖ 질량보존 검사  ───────────────────
   왼쪽:  H 4 · O 2          =          오른쪽:  H 4 · O 2   ✅ 균형!
```
- **반응물 슬롯**에 분자 카드를 놓고, **계수 스테퍼(+/−)** 로 개수 조절.
- **생성물**은 (a) 자동 도출 또는 (b) 플레이어가 선택.
- **좌우 원자 수를 실시간 표시**(저울 UI) → 원자 수가 같아야 **균형 ✅**.

### 판정 → 전투 연결
- **균형 정확** → 스킬 **풀 파워 + "질량보존" 크리 보너스**.
- **불균형(원자 수 안 맞음)** → "⚠ 질량보존 위반!" → **약발/실패**.
- 계수가 클수록(2H₂O vs H₂O) 위력↑ 하지만 **분자 재고를 그만큼 소모**(화학량론).
- **한계 반응물(limiting reagent):** 한쪽 분자가 부족하면 반응 규모 축소 → 생성물↓. (고급 개념, 실전 자원관리)

---

## 4. 전투 통합 아이디어
- **보스 약점 = 특정 반응식**. 예) 아쿠아리온: `2Na + 2H₂O → 2NaOH + H₂↑` 를 **균형 맞춰** 세워야 폭발 대미지.
- **계수 정확도 = 대미지 배율.** 반쯤 맞으면 반쯤, 완벽하면 크리.
- **연쇄:** 균형 반응식을 연속 성공하면 체인 보너스(기존 체인 시스템 재활용).
- **미션 강화:** "S1 미션 = 균형 잡힌 물 합성식을 완성하라(2H₂+O₂→2H₂O)" — 지금 미션 시스템에 자연스럽게 연결.

## 5. 기술적 기반 (이미 있음)
- **질량보존 검사기** = 밤샘 감사에 쓴 `chemaudit.js`의 파서(계수·아래첨자·괄호·이온 처리)를 그대로 in-game 함수로. → **좌우 원자 수 실시간 비교** 즉시 가능.
- 분자식↔원자수 변환(`subFormula`, `countAtoms`)도 이미 구현돼 있음.
- 즉 **엔진은 준비됨.** 필요한 건 UI(반응물/계수/저울)와 판정 연결.

## 6. 단계별 도입 (저위험 → 고완성)
- **1단계(작게):** 레시피 완성 시, 발사 전에 **"계수 맞추기 팝업"** 한 스텝 추가 — 균형 반응식의 계수를 플레이어가 채우면 **파워 보너스**. (현 시스템에 얹기만, 리스크 최소)
- **2단계(중간):** **분자 인벤토리** 도입. 원자→분자→(반응물로 저장). 반응식 세우기 슬롯 추가.
- **3단계(완성):** 저울 UI·한계 반응물·생성물 선택·보스별 요구 반응식. 완전한 화학량론 전투.

## 7. 리스크/주의
- **재미 유지:** 원자 조립의 촉각적 재미를 죽이지 말 것. 반응식 계층은 **깊이 추가**이지 대체가 아님.
- **난이도 곡선:** 1~2스테이지는 계수 1~2의 쉬운 식, 후반에 복잡한 식(Ca(OH)₂+CO₂→…). 계수 스테퍼로 진입장벽 낮춤.
- **UI 복잡도:** 세로 화면이 좁음 → 반응물 2칸 + 계수 + 저울을 컴팩트하게. PC(스팀) 타겟이라 여유 있음.

---
## 8. 한 줄 요약
> **"원자→분자(현행) 위에 '분자+계수→균형 반응식(신규)' 계층을 얹어, 질량보존을 맞춰야 강한 스킬이 나가게 한다. 균형 검사 엔진은 이미 있음. 1단계(계수 보너스 팝업)부터 점진 도입 추천."**

_작성: 2026-07-18 밤 (자율 작업). 구현 전 설계. 방향 확정되면 1단계부터._
