# 작업 이어가기 — 2026-07-21

새 대화창에서 이 문서만 읽고 바로 이어갈 수 있게 정리함.

배포 중: https://subrain-alchemist-game.web.app (전부 반영·검증 완료)

---

## ⚠ 시작 전 반드시 읽을 것

1. `Desktop\화학게임\에셋도구_사용법.md` — Flow·일레븐랩스 조작 매뉴얼 (실전 검증)
2. 이 문서의 "지금 하던 일" 섹션

---

## ✅ 끝남 — 희귀 카드 그림 9종 (2026-07-21 완료)

9종 전부 `assets/card/rare_<key>.jpg` (폭 600, 3:4) 로 들어갔고 `game.html` 에 붙었다.
H2·O2·CO·HCl(고급) / H2O·CO2·CH4·H2O2·NH3(희귀). 원본은 `raw_rare/*.png` (896×1200).

**✅ 배포 완료** — https://subrain-alchemist-game.web.app 에서 9종 전부 200 확인함.

### 영웅 카드 6종은 원본만 있음 (미적용)
`raw_rare/` 에 `rare_NaOH · CaOH2 · NaCl · CaCO3 · NaNO3 · Na2SO4` .png 가 있다.
아직 `card_trim.py rare` 를 돌리지 않았고 게임에도 안 붙었다 — **돌리면 자동으로 붙는다**(렌더 코드는 이미 있음).
남은 영웅 3종은 `CaCl2 · MgCl2 · KCl`, 전설 2종은 `H2SO4 · BaSO4`.

### ⚠ 뽑아둔 것 중 손볼 4장 (사용자 승인됨, 미착수)
원소 카드 12장과 나란히 놓고 보니 아래가 튄다. 자세한 건 `에셋도구_사용법.md` 의 분자 카드 섹션 참고.
| 카드 | 문제 | 바꿀 소재 |
|---|---|---|
| CH4 | 매듭띠가 없는 밋밋한 이중선 | 화로 위 푸른 불꽃 (형태 부여) |
| NaOH | 분홍·주황 성운, 고채도 | 접시 위 잿물 결정 + 옅은 열기 (절제) |
| NaNO3 | Na2SO4 와 똑같이 흰 결정 | 초석 결정에서 주황 불똥이 튀는 모습 |
| CaCO3 | Ca(OH)2 와 똑같이 유리컵 | 석회암 덩어리 + 바닥 앙금 |

### 🔑 비율 9:16 문제의 진짜 원인 (문서가 틀렸었음)
"설정이 저장 안 된다"가 아니었다. Flow에는 **비율 설정이 두 군데** 있고 서로 별개다.
- `⚙ 에이전트 설정 > 이미지 생성 기본값` — 3:4로 저장돼 있었다
- **프롬프트 바의 `Nano Banana 2` 칩** — 여기가 `1:1`이었고, 실제 생성은 이쪽을 쓴다

또한 **`에이전트` 칩이 켜져 있으면** 에이전트가 비율을 제멋대로 고른다(세로 프롬프트 → 9:16).
→ **`에이전트` 칩을 끄고, 그때 나타나는 `Nano Banana 2` 칩을 눌러 3:4·1x 를 확인**하면 896×1200 이 나온다.

### ⚠ 반드시 지킬 것 (영웅·전설 뽑을 때도 동일)
- **비율은 3:4 (896×1200)** — 원소 카드와 맞춰야 손패에서 안 잘린다.
- 프롬프트에 **`The card artwork FILLS 100% OF THE IMAGE, edge to edge, with NO table, NO surface
  and NO background`** 를 반드시 넣을 것. 빼면 **나무 탁자 위에 놓인 카드**로 나온다(O2에서 겪음).
- 프롬프트는 `find` 로 잡은 **ref로 클릭**할 것. 좌표 클릭은 창 크기가 바뀌어 자주 빗나간다.
- 입력 후 **스크린샷으로 확인하고** 생성 버튼을 누를 것.
- **이미지가 7~8장 쌓이면 스크린샷 CDP가 타임아웃**된다. 4~5장마다 새 프로젝트를 만들 것.
  (확인은 스크린샷 대신 **다운로드 후 로컬 PIL 미리보기**가 훨씬 안정적이다)
- 다운로드는 canvas 우회 + `md5sum` 중복 확인.
- 우하단 글자가 뒤집혀 보이는 건 **정상**(트럼프 카드 규칙, 180° 회전).

### 프롬프트 템플릿 (9종 전부 이걸로 뽑음, 검증됨)
```
A vertical tarot card for a fantasy alchemy game. The card artwork FILLS 100% OF THE IMAGE,
edge to edge, with NO table, NO surface and NO background visible behind or around the card.
Aged cream parchment card stock, rounded corners. An elegant SLENDER gold filigree border of
interlacing vine scrollwork frames a deep navy starfield center; small round gold medallions of
a sun, a crescent moon and a six-pointed star at the border midpoints. In the TOP-LEFT corner and
again in the BOTTOM-RIGHT corner rotated 180 degrees, a cream parchment shield panel holds the
molecular formula "<화학식>" in DEEP BLUE elegant serif lettering, like the rank on a playing card.
At the center, inside a faintly glowing circular alchemical diagram: <소재>.
Soft <등급색> aura. Softly painted 2D fantasy illustration, restrained and classical,
navy gold and cream palette. Only the text <화학식> appears, NO other words, NO watermark,
NO gemstones.
```
- 등급색: 고급 `EMERALD GREEN` · 희귀 `SAPPHIRE BLUE` · 영웅 `AMETHYST VIOLET` · 전설 `RADIANT GOLD`
- **소문자가 든 화학식은 대소문자를 명시할 것.** HCl 은 이렇게 해서 성공했다:
  `the formula is spelled capital H, then capital C, then a LOWERCASE letter l (a lowercase L, never a capital I)`
  → NaCl·CaCl2·MgCl2·KCl·NaOH 도 동일하게 처리해야 한다.

### 후처리 파이프라인 (그대로 재사용)
1. `raw_rare/rare_<key>.png` 로 모은다 (896×1200)
2. `python tools/card_trim.py rare` → `assets/card/rare_<key>.jpg` (폭 600, q86, 장당 ~130KB)
   - `rare` 인자 모드를 이번에 추가함. 여백이 0~35px로 제각각이라 실측 트림이 필요하다.
3. `game.html` 렌더는 이미 붙어 있다 — `rare_<key>.jpg` 가 있으면 자동으로 깔린다.
   그림이 붙으면 `hasArt` 클래스가 붙어 **가운데 분자식 글자를 숨긴다**(그림의 좌상단 방패가 대신함).
4. `cp assets/card/rare_*.jpg dist/assets/card/` → `firebase deploy --only hosting`
5. 배포 후 `curl -o /dev/null -w "%{http_code}"` 로 200 확인

---

## 완성된 것 (이번 세션)

| 기능 | 내용 |
|---|---|
| 🎴 희귀 카드 **시스템** | 5등급(일반+고급·희귀·영웅·전설). 확률 = 5%+레벨×2%(+행운의 시약 8%p), 최대 40%. 등급은 레벨로 잠김. 클릭 시 **원소 소모 없이 즉발**. 10만회 시뮬 검증 |
| 🔄 카드 교환(멀리건) | **우클릭으로 카드 잠금** → 🔄 버튼으로 나머지만 재추첨. 전투마다 리셋, 레벨 5·8에서 +1. 교환 시 희귀 카드도 재추첨(희귀 낚시) |
| ⚗ 첫 합성 애니메이션 | 게임 통틀어 첫 합성 시 전투 정지 → 원자가 모여 분자 결합 → **클릭 전까지 유지** + 스킬 효과 설명. 한 번만(세이브 기록) |
| ➕ 연쇄 합성 | `H H O ＋ C O` 로 두 분자 연달아 발사. ＋도 슬롯 1칸 차지 |
| 🃏 카드 손패 | 트럼프처럼 겹쳐 듦. 커서 올리면 부채꼴로 펴짐. 낸 카드는 위로 삐져나옴 |
| UI 재배치 | HP → **왼쪽 세로바**(30% 이하 붉게 깜빡), 에테르 → **하단 왼쪽**(배지와 겹침 해소), 텍스트 전체 확대 |
| 정리 | 보물 노드 → 휴식으로 교체. 원소 가방 → **여벌 플라스크**(교환 +1)로 교체 |
| 퀴즈 | 15문항 다 볼 때까지 중복 없음 |

### 주요 코드 위치 (game.html)
- `RARITY` / `RARE_TIER` / `rollRare` / `rollRareHand` — 3455~3495행 근처
- `fireRare` — `endPlayerAction` 바로 뒤
- 희귀 카드 렌더 — 3384행 근처 (`rc.className='coin rareCard'`)
- `synthCelebration` + `RECIPE_FX` — 1988행 근처
- `doSwap` / `toggleHold` — `handCount` 뒤
- `WIRE_SLICE` — 철사별 border-image 슬라이스 실측값

---

## 남은 할 일 (우선순위 순)

> 사용자 방침 (2026-07-21): **그림보다 기능이 우선.** 그림은 폴백이 있어 없어도 돌아간다.

1. ~~**실패 힌트**~~ ✅ **끝남 (2026-07-21, 배포됨)** — `failHint()` 함수. 아래 상세 참고
2. **도감을 카드 형식으로 / 웹오브에테르** ← 사용자 아이디어(2026-07-21), 미착수. **이제 이게 1순위**
   - 원소=노드, 반응식=간선인 네트워크 UI. 오늘 만든 우측 `#sidePanel` 이 그 자리가 될 수 있다
   - 카드 그림이 21종(원소 12 + 분자 9) 배포돼 있으니 도감을 카드 갤러리로 만들면 그림이 제값을 한다
   - `codexScreen` 이 현재 어떻게 그려지는지부터 볼 것
3. **영웅·전설 카드 그림** — 6종은 `raw_rare/` 에 원본 있음(트림만 하면 붙음), 5종(CaCl2·MgCl2·KCl·H2SO4·BaSO4) 미생성
4. **튀는 4장 재생성** (CH4·NaOH·NaNO3·CaCO3 — 위 표 참조)
5. **에테르 정령 이미지** 재생성 (지금 🏺 이모지 폴백)
6. **＋카드 이미지** 재생성 (지금 금색 ＋ 문양)

### ✅ 실패 힌트 — 구현 완료 (2026-07-21)

`failHint(atoms)` — `recipeFor` 바로 아래. 조합 실패 시 로그에 한 줄 더 붙는다.
```
반응식이 아니라서 원소를 그냥 던졌다… 11 피해.
💡 산소(O) 1개 더 → 물 H₂O
```
- 원자 수가 **1~2개 차이**나는 반응식을 찾아 부족분·초과분을 짚어준다
- 차이가 적은 것 우선, 같으면 **이미 아는 반응식**을 앞세운다(이름을 말해줄 수 있어 더 잘 가르침)
- ⚠ **미발견 반응식은 이름을 감춘다** → `💡 산소(O) 1개 더 → ??? (아직 모르는 반응식)`
  기존 "미발견 조합식은 숨긴다" 원칙과 같은 이유 — 이름까지 주면 직접 발견하는 재미가 사라진다
- ⚠ `locked` 반응식(화학자 전수 전)은 후보에서 아예 뺀다
- 조사(이/가·을/를)는 화학식이 로마자로 끝나 안 붙으므로 **화살표 형식**으로 쓴다
- 슬롯은 발사 시 비워지므로 `state._lastUsed`(doFire 가 기억)로 힌트를 만든다

**아직 안 한 것:** 발사 *전*에 슬롯 힌트(`#slotHint`)로 미리 알려주는 것.
지금은 발사 후에만 알려준다 — 미리 알려주면 손을 너무 잡아주는 느낌이라 일부러 뺐다.

### 외부 기획 논의 반영 결과 (2026-07-21) — **다시 논의하지 말 것**

다른 대화에서 나온 화학 반응식·보스 설계안을 코드와 대조했다. **대부분 이미 구현돼 있었다.**

| 제안 | 실제 |
|---|---|
| 2H₂+O₂→2H₂O / CH₄+2O₂ / HCl+NaOH / CaCO₃→CaO+CO₂ | **4종 전부 이미 있음** |
| "석회암 보스" | **8번 라임 가디언** — HCl 용해 + 온도 80↑ 열분해 두 경로 다 있음 |
| "아연 골렘 Zn+2HCl→ZnCl₂+H₂↑, 수소 포집 후 폭발" | **6번 쿠퍼 골렘**이 동일 메커니즘. `state.h2`+`tryIgniteH2()`+`h2_ignite` 콤보로 구현됨 |
| "보스 10~12종 설계 가능" | **이미 9종** (물질의 상태·발열흡열·알칼리금속·산과염기·산화환원·금속반응성·앙금·탄산칼슘·탄소동소체) |
| "온도·산소포화도·압력 환경 변수" | 온도·산소·pH·젖음 **이미 있음**. 압력만 없음 |

**버린 것과 이유**
- **압력** — 기존 변수 3개와 곱해져 복잡도만 폭증. 학습 효과는 온도·pH보다 약함
- **Zn(아연)** — 금속 반응성은 Na·K·Mg·Fe·Cu로 이미 충분히 다룸. 원소만 늘어남
- **광합성·호흡 C₆H₁₂O₆** — 원자 24개라 슬롯 6칸 시스템에 안 들어감
- **게이트 퍼즐** — `labScreen`(퀴즈)·`sepScreen`(분리)이 이미 그 역할. 확장은 비용 대비 낮음
- **판매량·수익 예측** — 검증 불가 수치. 오푸스 마그넘 35만 장은 장르 최상위 사례라 기준선으로 부적절

**가져갈 것 하나** — **웹오브에테르 도감**(원소=노드, 반응식=간선인 네트워크 UI).
위 2번 "도감을 카드 형식으로"와 같은 갈래이고, 오늘 만든 우측 `#sidePanel` 이 그 자리가 될 수 있다.

> ⚠ 위 "압력 버림" 판단은 사용자가 **뒤집었다**(2026-07-21). 전역 변수로 넣기로 결정 → 아래 참고.

---

## ✅ 🔧 화면 배치 편집기 (2026-07-22, 배포됨)

**https://subrain-alchemist-game.web.app/editor.html** (로컬은 `editor.html`)

"매번 말로 설명하지 않게 별도 편집기를 만들어달라"는 요청.
게임을 **iframe 으로 띄우고 그 위에서 직접 끌어 배치**한 뒤, 결과를 코드로 내보낸다.

- 왼쪽: 진짜 게임(iframe). 오른쪽: 조작 패널
- `⚔ 전투 화면` — 모든 요소가 보이도록 데모 전투를 세팅
- `✋ 배치 모드` — 요소를 끌어 옮김 (게임 안의 `layToggle` 을 그대로 호출)
- 요소 목록에서 **X/Y 를 숫자로 직접 입력**해 미세 조정 가능
- `📋 내보내기` — `const LAYOUT_DEFAULT = { '.bossCard': {x:22.25, y:54.37}, ... }` 형태로 뽑힌다.
  **이걸 그대로 전달하면 기본 배치로 반영하면 된다**
- `↺ 기본값` — 전부 초기화

### 게임 쪽 대응
- 상단바 `🔧` 버튼으로 게임 안에서도 배치 모드를 켤 수 있다
- 좌표는 부모 대비 **%** 로 `localStorage['cq_layout_v1']` 에 저장 (창 크기 바뀌어도 유지)
- ⚠ **저장된 항목만 `absolute`** 로 바꾼다 — 안 옮긴 요소는 원래 흐름 그대로라 화면이 안 깨진다
- ⚠ `const`/`let` 은 `window` 에 안 올라가서 편집기가 못 읽는다 →
  `Object.assign(window, {...})` + `layout` 은 재대입되므로 getter/setter 로 노출

### ⚠ 아직 안 고친 것 — 넓은 화면 겹침
1400px+ 에서 **보스 카드와 플레이어가 98px 겹친다**(실측). 보스를 키우면서 아레나 높이를 안 늘렸다.
사용자가 편집기로 직접 배치하기로 해서 수치 조정은 보류 중.

## ✅ 🏺 에테르 초상 이미지 (2026-07-22, 배포됨)

`assets/ui/ether.jpg` (160×160, 6.6KB). 원본 `raw_ui/ether.png` (1024×1024, 1:1).
⚠ **설정상 에테르는 '항아리'가 아니다** — 자기 생명을 재료로 삼아 몬스터를 흩어놓고 스러진
늙은 연금술사의 영혼이다. 기존 🏺 이모지는 설정과 안 맞았다.
→ 후드 쓴 노인이 금빛으로 빛나며 가장자리가 빛 알갱이로 흩어지는 초상. 54px 로 줄여도 형체가 읽힌다.

### ⚠ 여기서 스크립트 전체를 죽였다 (TDZ)
이미지 로더를 `(function(){...})()` 즉시 실행으로 넣었는데, 그 위치(4576행)가
`const ASSET_VER`(4886행)보다 **310줄 앞**이라 TDZ ReferenceError 가 나면서
**그 아래 전부가 초기화되지 않았다**(`POSES` 등). 화면은 뜨는데 전투가 안 됐다.
→ `document.addEventListener('DOMContentLoaded', ...)` 로 미뤄 해결.
**교훈: 파일 중간에 즉시 실행 코드를 넣을 때는 참조하는 const 의 선언 위치를 먼저 확인할 것.**

## ✅ 🏺 에테르 정령 조언 강화 (2026-07-22, 배포됨)

### ⚠ 버그 2개를 잡았다
1. **`ignis_o2` 경고가 한 번도 안 떴다.** 슬롯에는 `'O'` 같은 **문자열**이 들어가는데
   `s.slots.filter(x=>x&&x.sym==='O')` 로 객체 속성을 보고 있었다 → 항상 0.
2. **보스별 조언이 나머지를 전부 덮었다.** `golem_metal`(pri 98) 처럼 **항상 참**인 조언이 있어
   우선순위만 보면 그것만 무한 반복되고 아래 상황 조언은 영원히 안 나왔다.
   → `etherSeen`(최근 6개)을 두고 **안 본 것부터** 고르게 함. 다 보면 한 바퀴 리셋.

### 새 조언 (오늘 추가한 메커닉 반영)
| id | 조건 | 내용 |
|---|---|---|
| `slot_ready` | 조합 완성 | 합성 버튼을 눌러라 |
| `slot_multi` | 정수배로 놓음 | 계수를 맞춘 만큼 위력이 곱해진다 — 이것이 계수비 |
| `slot_near` | 한두 개 모자람 | **실시간 실패 힌트를 그대로 보여준다** (`{HINT}` 치환) |
| `rare_card` | 희귀 카드 보유 | 슬롯에 놓고 ➕ 로 이을 수 있다 |
| `steam` | 젖음 + 온도 60↑ | Mg·Fe 수증기 반응 기회 |
| `pres_high` / `pres_low` | 압력 130↑ / 80↓ | 기체 위력 증감 (`{PRES}` 치환) |

순간적 '기회' 조언은 보스별 상시 조언보다 우선순위를 위로 뒀다(steam 86, pres_high 78).

**실측 순환**: `golem_metal → steam → slot_near(💡수소 1개 더 → 물 H₂O) → rare_card → wet_alkali → default → 순환`

## ✅ 시스템 4종 (2026-07-22, 배포됨)

**1. 죽은 코드 제거** — `fireRare()` 삭제 (희귀 카드가 슬롯 방식으로 바뀌며 호출처가 사라짐)

**2. ⚡ 콤보 시연** — 도감 콤보 항목에 `▶ 콤보 시연 보기`
- 콤보는 원자 조합이 아니라 **조건부 발동**이라 분자 시연을 그대로 못 쓴다
- `comboTokens(eq)` — 반응식 좌변을 화학종으로 쪼갠다: `2Na + 2H₂O → …` → `['2Na','2H₂O']`
- `synthCelebration` 에 `opts.tokens` / `opts.fx` 추가. 라벨이 길어 `.celeAtom.wide`(알약 모양) 사용
- 색은 라벨에서 첫 원소기호를 찾아 `ELEMS[k].color` 사용

**3. 🎯 보스 공략 브리핑** — 상단바 `🎯` 버튼 (전투 중 언제든)
로딩 화면에만 있던 정찰 보고를 전투 중에도 볼 수 있게 했다. 막히는 지점이 바로 여기다.
약점 · 관련 반응식 · 조언 · **지금 전장(온도/압력/O₂/젖음/pH)** · 화학 노트 · 보유 반응식 목록.
⚠ `hideWeak` 보스는 약점을 계속 감춘다(분광기·화학자 필요).

**4. 💬 전투 평가 코칭** — 점수만 보여주면 "C 받았네"로 끝난다. 무엇을 바꿔야 오르는지 알려준다.
`evalCoach(e)` — 우선순위대로 **가장 중요한 하나**만 짚고, 다음 등급까지 남은 점수를 표시.
```
[D] → 💬 역효과 스킬을 2회 썼다. 상단 🎯공략에서 약점을 먼저 확인하자. (다음 등급까지 39점)
[B] → 💬 ⚡콤보를 한 번도 못 썼다. S 등급은 콤보가 필수다. (다음 등급까지 1점)
[S] → 🌟 흠잡을 데 없다.
```

## ✅ 🎴 희귀 카드 = 슬롯에 놓는 분자 토큰 (2026-07-22, 배포됨)

"희귀 카드는 ➕ 로 올리는 건데 왜 바로 나가지?" — 맞는 지적. 카드인데 손에서 즉발로 나가버려
**➕ 연쇄에 낄 수가 없었다.** 원소 카드와 같은 흐름으로 맞췄다.
- 슬롯에 `@H2O` 같은 **분자 토큰**으로 놓인다 (`RARE_MARK='@'`, `isRareTok()`, `tokKey()`)
- `groupRecipe(g)` — 묶음이 희귀 토큰 하나면 그 분자, 아니면 기존 원자 판정.
  `matchRecipeAny` / `matchChain` 이 이걸 쓴다
- **희귀 카드는 다른 원소와 한 묶음에 못 섞는다** (이미 완성된 분자라서)
- `slotAtoms()` 에서 토큰 제외 — 원소를 소모하지 않는다
- 카드는 **발사될 때(doFire) 비로소 손패에서 빠진다.** 슬롯에 올려둔 상태로 되돌릴 수 있다
- 슬롯 칸에 등급색 테두리 + 분자식으로 표시

**실측**: `H₂O(희귀) ＋ C O` → 연쇄 성립, **79 피해**, 소모 원소는 `C1 O1` 뿐, 카드는 발사 후 소멸.
⚠ 옛 `fireRare()` 는 이제 호출되지 않는 죽은 코드다 (지우지 않고 남겨둠 — 정리 필요).

## ✅ 🏕 휴식 노드 폐지 → 🧪 화학 실험실 (2026-07-22, 배포됨)

승리 시 이미 완전 회복이라 휴식이 겹쳤다. 그 자리에 **구현만 돼 있고 한 번도 배치되지 않던**
`lab`(퀴즈 3문항) 노드를 넣었다. 죽은 콘텐츠가 살아났다.
- `labFinish()` 보상도 **원소 → 확률 강화**로 교체 (원소 보상 폐지 방침과 맞춤)
  정답당 `🎴 희귀 확률 +2%p · 💠 귀한 원소 등장률 +2%p`, 3문항 정답이면 도구까지

## ✅ ▶ 도감 합성 시연 + 스킬 효과 (2026-07-22, 배포됨)

"스킬 설명이 확실해야 재밌다"는 요청. **새로 만든 게 거의 없다 — 이미 있던 걸 재사용했다.**
- `synthCelebration()` (원자가 날아와 결합하는 연출) — 전투 중 '첫 합성' 때 **한 번만** 쓰이고 있었다
- `RECIPE_FX` (반응식별 스킬 효과 설명) — 이미 20/24 작성돼 있었다

**한 일**
1. `synthCelebration(recipe, done, opts)` — `opts.title/lesson/btn` 로 문구를 갈아끼울 수 있게 함
2. 도감 그물에서 **발견한 분자**를 누르면 조합·반응식·**효과**가 다 뜨고 `▶ 합성 시연 보기` 버튼
3. 누르면 도감을 잠시 덮고 연출 재생 → `◀ 도감으로 돌아가기` 로 복귀
4. 빠져 있던 `RECIPE_FX` 4종(Na·K·Mg·Fe 투척) 작성 — 조건부 반응이라 설명이 특히 필요했다

### ⚠ 여기서 잡은 버그 — 원소 'Na' 와 분자 'Na' 가 키 충돌
`webSel` 에 키만 저장하니 분자 노드를 눌러도 `ELEMS['Na']` 가 먼저 걸려 **원소 정보만 나왔다**.
→ `'el:Na'` / `'mol:Na'` 처럼 **타입을 붙여** 저장하도록 9곳 수정. Na·K·Mg·Fe 4종이 해당된다.

## ✅ 🖥 넓은 화면 확대 (1400px+) (2026-07-22, 배포됨)

1920 모니터에서 1100px 만 쓰고 양옆이 놀았다. 넓은 화면에서만 키운다(1366 노트북은 그대로).
| | 이전 | 지금 |
|---|---|---|
| 게임 / 패널 | 760 / 320 | **1080 / 400** |
| 손패 카드 | 82×110 | **121×146** |
| 슬롯 | 62 | **84** |
| 보스 카드 / 그림 | 352 / 160 | **470 / 230** |
| 플레이어 | 145 | **200** |

### ⚠ 미디어쿼리는 특정도를 올리지 않는다
처음엔 셸만 커지고 카드·보스는 그대로였다. `@media` 블록을 기본 규칙보다 **앞**에 뒀더니
나중에 나오는 `.coin{width:82px}` 에 그냥 덮였다. → **스타일시트 맨 끝으로 옮겨야 한다.**

## ✅ 🔓 잠긴 반응식도 스스로 합성 가능 (2026-07-22, 배포됨)

**아는 사람이 정확히 조합했는데 막히면 답답하다**는 지적 반영.
예전엔 `locked` 반응식 5종(`NaOH · Ca(OH)₂ · HCl · NH₃ · H₂SO₄`)을 화학자에게 배우기 전엔 무조건 막았다.
```js
function matchRecipe(){ return matchRecipeAny(); }   // 잠금 차단 제거
```
- **정확한 원자 구성을 놓는 것 자체가 아는 것의 증거**로 본다
- 스스로 맞히면 특별 대우 — `📜 스스로 알아냈다!` + **XP 80**(일반 발견 50)
- 슬롯 힌트에도 `📜 스스로 알아냈다!` 가 미리 뜬다
- `failHint` 후보에도 포함시켰다. 단 **이름은 계속 감춘다** → `수소(H) 1개 더 → ??? (아직 모르는 반응식)`
- **화학자는 여전히 의미 있다** — 모르는 사람에게 공짜로 주고 XP·도구·원소 해금을 함께 준다
- 도감 문구도 수정: `어려운 반응식. 직접 맞혀도 되고, 붙잡힌 화학자에게 배워도 된다`

## ✅ 🌊 아쿠아리온 밸런스 — 3턴 컷 해결 (2026-07-21, 배포됨)

`alwaysWet` + `mine`에 Na·K 라 "캐서 던지기"만 반복하면 3턴에 끝났다. 두 가지를 같이 손봤다.

1. **`dryOutField()`** — 알칼리 폭발이 주변 물을 써버려 전장이 마르고, 물 보스는 **다음 턴 하나를 건너뛰어야** 다시 젖는다(`state.wetCd`). 화학적으로도 2Na + 2H₂O 가 물을 소비하는 게 맞다.
2. ⚠ 조건을 **`B.key==='aquarion' || state.wet` → `state.wet`** 으로 바꿨다.
   전자를 두면 물이 말라도 아쿠아리온이면 무조건 터져서 1번이 무력화된다.
3. 아쿠아리온 전용 보정 **Na 48→30, K 60→38**.
   약점 확정 크리 ×1.8 이 붙어 48이 곧 86이 되는 바람에 2방이면 끝났기 때문.

**실측**: 3턴 → **5턴** (`54 / 0 / 54 / 0 / 54`). 마른 턴 2번이 강제로 생겨 다른 수단을 쓰게 된다.
**소프트락 없음** — `mine` 에 Na·K 를 남겨 안전망 유지. 마른 턴에도 H₂O·H₂O₂·NaOH 등을 만들 수 있다.

## ✅ 🖥 PC 2단 레이아웃 (2026-07-21, 배포됨)

스팀 출시가 목표인데 모니터에서 세로 상자 하나만 띄우니 양옆이 통째로 놀았다.
`.shell` 로 감싸 **왼쪽 게임(760px) + 오른쪽 도감 패널(320px)** 2단으로 만들었다.
좁은 화면(≤1140px)에서는 세로로 접힌다.
- 우측 `#sidePanel` = **배운 반응식 칩 전용**. 칩을 누르면 슬롯에 장전된다
- 오버레이(지도·도감·상점·인트로) 표시 중엔 `.shell:has(...)` 로 패널 자동 숨김
  ⚠ `#introScreen` 은 클래스가 아니라 인라인 `display` 로 열려서 `.show` 토글을 따로 붙였다
- ⚠ **`.shell{width:min(1100px,100%)}` 를 지울 것 (`.game` 이 318px로 무너진다)** — 위 그물 섹션 3번 참고

### ⚠ 전투 로그는 원래 자리(보스 옆)에 둔다 — 옮기지 말 것
한때 "보스 아트를 가린다"는 이유로 우측 패널로 옮겼으나 **사용자가 원래대로 되돌리라고 했다**(2026-07-21).
`position:absolute; top:48px; right:10px` 로 보스 옆에 떠 있는 게 맞다. 글자를 키운 만큼 폭만 250→290px.

## ✅ 글자 크기 일괄 확대 (2026-07-21, 배포됨)

"눈에 잘 안 들어와서 정보를 얻기 힘들다"는 지적 반영. **124곳**을 키웠다.
작은 글자일수록 더 많이 올리고, **26px 이상(연출용 대형 문구)은 건드리지 않았다.**
```
8→11  9→12  10→13  11→14  12→15  13→16  14→17  15→18
16→19  17→20  18→21  19→22  20→23  22→25  24→27
```
- 가장 많던 10~13px(80곳)이 13~16px가 되어 본문 가독성이 확 올라간다
- ⚠ 같이 손봐야 했던 것: `.mapNode` 폭 74→124px, `.mapRow` gap 46→16px.
  안 그러면 '화염의 지배자'가 **'화염의 지배 / 자'** 로 잘린다
- `.coin` 이 넘침으로 잡히는 건 정상 — 호버 라벨(`bottom:-19px`)이 카드 밖에 있게 설계돼서다

## ✅ 🕸 에테르의 그물 — 도감 네트워크 (2026-07-21, 배포됨)

도감 맨 위에 SVG 네트워크. **원소 12종은 바깥 고리, 분자 24종은 구성 원소의 무게중심**에 놓고 간선으로 잇는다.
목록만 보면 반응식 24개가 나열될 뿐이지만, 그물로 보면 "이 원소가 어디 쓰이는지"가 한눈에 들어온다.
- `webLayout()` / `renderWeb()` / `webInfoText()` — `openCodex` 바로 위
- 원소 클릭 → 그 원소가 들어가는 아는 반응식 강조 + `수소(H) — 아는 반응식 4/9개에 쓰인다`
- 분자 클릭 → 조합·반응식. 미발견은 `?`/`🔒` 로만 보이고 간선도 흐리다(발견 재미 보존)
- **배치는 결정적**이다 — 물리 시뮬을 쓰면 열 때마다 모양이 달라져 지도처럼 외울 수가 없다

### 여기서 걸린 함정 3가지 (다시 겪지 말 것)
1. **SVG `<text>` 안에 `subFormula()` 를 넣으면 안 된다.** `<span class="sub">` 를 뱉는데 SVG 안에선
   유효하지 않아 파싱이 깨지고 **노드가 통째로 사라진다**(원소 12개가 0개로 나왔다).
   → 유니코드 아래첨자를 쓰는 `subFormulaPlain()` 을 따로 만들었다.
2. **겹침 해소 뒤에 '고리 안쪽으로 가두기'를 한 번만 하면 가두는 순간 다시 겹친다.**
   두 제약을 매 반복마다 번갈아 적용해야 수렴한다. (최소간격 14.6 → 33.9, 겹침 4쌍 → 0)
3. **`.shell` 에 명시적 폭이 없어 `.game` 이 318px 로 무너져 있었다.** ← PC 레이아웃 전체에 영향
   `body{display:flex}` 안에서 `.shell` 폭이 auto 면, 자식의 `width:min(760px,100%)` 에서
   100% 가 참조할 기준이 순환한다. → `.shell{width:min(1100px,100%)}`, `.game{width:760px}` 로 고정.

## ✅ ⚖ 배수 합성 — 계수비 학습 (2026-07-21, 배포됨)

"시험은 계수비를 묻는데 게임은 분자식만 묻는다"는 지적 반영.
**정수배로 놓으면 그만큼 세진다.** `H·H·O` 를 두 벌(H×4, O×2) 놓으면 그게 곧 `2H₂ + O₂ → 2H₂O` 의 좌변이다.
```
⚖ 2배 합성! 2H₂ + O₂ → 2H₂O — 계수를 2배로 맞췄다. 위력 ×2
```
- `recipeMultiple(atoms)` — `matchRecipeAny` 바로 위. 원소 종류가 같고 **모든 원소가 같은 배수**여야 성립
- 배율은 `state.eqBonus` 에 곱해 반영 (dealBoss 가 이미 쓰는 배율)
- ⚠ **연쇄(➕)에는 적용 안 함** — 두 묶음 × 배수까지 겹치면 계산이 너무 복잡해진다
- ⚠ 슬롯이 비워지기 전에 `doFire` 첫 줄에서 미리 배율을 재둔다

**슬롯 성장 조정 (같이 배포)** — Lv6·Lv10 에 슬롯 +1 추가
| | 6칸 | 7칸(Lv4) | 8칸(Lv6) | 9칸(Lv8) | 10칸(Lv10) |
|---|---|---|---|---|---|
| 2배 가능 | 15/24 | 15 | 17 | 17 | **21/24** |

10칸이 의미 있는 지점 — 원자 5개짜리(CH₄·CaCO₃·NaNO₃·Ca(OH)₂)가 열린다.
`H₂SO₄`·`Na₂SO₄`(각 7원자→14칸)는 끝까지 2배 불가.

## ✅ 반응식 칩 = 원클릭 장전 (2026-07-21, 배포됨)

"물 한 번 쏘려고 매번 H·H·O 를 하나씩 놓는 게 번거롭다"는 지적 반영.
**처음 한 번은 직접 조합해 배우고, 배운 뒤로는 우측 패널의 칩 한 번으로 슬롯에 장전된다.**
- `loadRecipeToSlots(r)` — `renderSkillChips` 바로 위
- ⚠ 미발견 반응식은 애초에 칩이 안 뜨므로 **직접 발견하는 재미는 그대로**다
- 슬롯은 발사 전까지 원소를 소모하지 않으므로 덮어써도 잃는 게 없다 → 확인 없이 바로 장전해도 안전
- 원소가 모자라면 **기존 슬롯을 건드리지 않고** 부족분만 알려준다 (`손패 부족 — H 2개 필요(보유 1)`)
- 슬롯 칸보다 큰 반응식이면 `슬롯 N칸이 필요하다 (현재 M칸)` 안내

## ✅ 🗺 지도가 시작 지점부터 보이게 (2026-07-21, 배포됨)

지도는 아래→위 구조인데 열면 맨 위(보스)가 보여 "어디로 가야 하지?"가 됐다.
⚠ **`requestAnimationFrame` 한 번으로는 안 된다** — 그 시점엔 노드 이미지가 로드 전이라
`scrollHeight == clientHeight` 라 스크롤 여지가 없어 `scrollTop` 이 0에 머문다.
→ rAF + `setTimeout(80/250)` + **이미지 `load` 이벤트**까지 걸어 세 번 내린다.

## ✅ 🎈 압력 — 구현 완료 (2026-07-21, 배포됨)

**따로 노는 변수가 되지 않게 온도에서 파생시켰다.** 독립 변수로 두면 외울 게 하나 더 느는 꼴이 된다.
```js
function pressureNow(){ return clamp(Math.round(100 + (state.temp-50)*0.8 + (state.gas||0)), 30, 200); }
function gasMult(){ return clamp(pressureNow()/100, 0.7, 1.4); }   // 기체 스킬 위력 배율
```
- **게이뤼삭 법칙** — 밀폐된 전장에서 온도↑ → 압력↑. 온도 배지 옆에서 같이 움직인다
- `state.gas` — 반응식에 **`↑` 가 있는 반응**(H₂↑·CO₂↑ 등)이 8씩 올린다. 데이터에 이미 있던 표기를 그대로 읽는다
- 보스 턴마다 3씩 빠진다(환기)
- **효과**: `tags:['gas']` 스킬 위력 ×0.7~1.4. 태그는 이미 11곳에 있어서 그대로 물렸다

**실측** — CO₂(gas 태그)로 같은 조건에서: 저온20/압력68 → **14 피해**, 기본50/압력92 → **18**, 고온95/압력132 → **25**.
→ "온도를 올려 압력을 키운 뒤 기체를 쏜다"가 새 정석 루트가 된다.

### 참고
- `bgm_defeat.mp3` 없음 (일레븐랩스 음악은 900크레딧/분이라 보류 중)
- fx 이미지 12종은 `tools/fx_sheet.py` 로 코드 생성 완료 (Flow로는 못 만듦 — 애니메이션 스트립)

---

## 검증 습관 (지킬 것)
- 코드 수정 → `node --check` 로 문법 확인
- 로직은 **헤드리스 Node 스텁**으로 먼저 검증 (확률·드로우 등)
- 브라우저에서 실제 동작 확인 (`python -m http.server 8899`)
- 배포 후 **curl 200 확인** (복사 누락 잦음)
- ⚠ **소리는 확인 불가** — 효과음은 사용자가 최종 판단

## 🖼 전투 화면 구도 — 대각선(JRPG 표준) 확정 (2026-07-22)

**문제:** 광폭(1400px+) 화면에서 **플레이어가 보스를 가림**.
실측 원인 — 보스326 + 플레이어269 + 여백20 = **615px** 인데 아레나는 **573px**뿐 → 34px 겹침.
아레나는 가로 1077px인데 둘 다 `left:50%`로 **세로 한 줄에만 쌓고 있었음**(가로 낭비).

**해결:** 세로 스택 → **대각선 구도**로 변경 (`@media (min-width:1400px)` 안에서만).
- `.bossCard{left:70%}` — 보스 우상단, 470px / 테두리 30px
- `.playerZone{left:21%}` — 플레이어 좌하단, 이미지 210px
- 실측 결과: **겹침 0**, 둘 사이 **212px 간격**(투사체 이동 공간), 아레나 밖으로 안 나감, 세로 스크롤 없음
- 가로로 분리했으므로 **구조적으로 겹칠 수 없다** — 앞으로 스프라이트를 키워도 안전

**함정 (반드시 기억):**
1. `left`만 바꾸고 **`transform:translateX(-50%)`는 그대로 둘 것.**
   `shake()`(약 2290행)와 `applyLayout()`(약 2291행)이 그 값을 하드코딩 전제로 깔고 있어서,
   transform을 없애면 피격 흔들림마다 요소가 화면 절반만큼 튄다.
2. `floatText()` / `burst()`가 `calc(50% + 95px)`처럼 **중앙 기준 하드코딩**이었음 →
   대각선 구도에선 데미지 숫자가 엉뚱한 곳에 뜬다. **`fxAnchor(atBoss)`** 헬퍼를 추가해
   실제 대상 `getBoundingClientRect()`에서 좌표를 뽑도록 교체함. 좁은 화면에도 그대로 맞는다.
3. 좁은 화면(1280 등)은 미디어쿼리 밖이라 **기존 세로 구도 유지** — 확인함(겹침 0, 스크롤 없음).

**검증:** 1920×1080 실제 전투로 H₂O 발사 → 데미지 65 정상, 데미지 숫자가 보스 카드 안(x1205, 보스 920~1390)에 표시.
※ 발사 직후 HP가 안 깎이면 버그가 아니라 **계수비 퀴즈 대기 상태**임(`#balSkip`).

**배포 완료** — https://subrain-alchemist-game.web.app (HTTP 200, 신규 코드 확인)

---
## 2026-07-24 자율 작업 (사용자 퇴근 중)
**배포 완료:**
- ⚖ **계수 저울 미니게임** — 텍스트 퀴즈 폐지. lab 노드가 이제 '겪는' 실험: ＋/－로 계수를 조절해
  양팔(반응물/생성물) 원자 수를 맞춘다. 저울(assets/ui/balance_scale.png)이 무거운 쪽으로 기울고,
  균형+최간단(gcd=1)이면 금빛 수평. 도전 8종(물·Mg연소·C연소·메테인·암모니아·CO연소·Na+물·철산화).
  BALANCE_CHALLENGES / showLab·renderLab·balUpdate·labCheck. 보상(labFinish)은 유지.
- 🐉 **보스 카드 3종 설치** — bcard_ignis(앵커)·bcard_phantom·bcard_lime (assets/card/). 지배자 카드 도감에 표시.
**남은 것 (사용자 직접):**
- 보스 카드 6종(ACID·FROST·GOLEM·FERRUM·AQUARION·DRAGON)이 **Flow 프로젝트 eb0951eb(05:24)**에 있음.
  그 프로젝트 렌더러가 얼어(이미지 7~8장) 내가 못 받음. 사용자 브라우저로 받아 assets/card/bcard_<보스>.jpg 로 넣으면
  도감 9/9 완성. (파일명 무관 — 그림 보고 매칭 가능)
- 잡몹 20종 카드는 미착수.

---
## 2026-07-24 저녁 배포 (v15)
- **보스 카드 5종 회수·설치**: 얼어붙은 Flow 프로젝트(eb0951eb) DOM에서 media URL fetch→다운로드로 회수 성공.
  - 설치됨: frost·acid·aquarion·ferrum·golem (bcard_*.jpg, 폭600) → 이제 보스 카드 총 8종.
  - ⚠ **DRAGON만 그 프로젝트에 없었음** → 카드 미제작. 다음에 dragon 타로 카드 1장만 뽑아 assets/card/bcard_dragon.jpg 넣으면 9종 완성.
- **지도(mapScreen) 배경 추가**: 세로 스크롤 노드맵이 단색이었음 → map_world.jpeg를 어둡게(linear-gradient 0.80~0.90) fixed cover로 깔음.
- **노드 아이콘 이모지→이미지**: 기존 아트 재활용(assets/node/*.png, 이모지 폴백)
  - 시작지점 ← player_front_extra.png, 화학실험실 ← balance_scale.png(저울), 분리실험실 ← distill_bg.jpg(증류)
  - 모닥불(rest)은 캠프 아트 없어 🏕 이모지 유지 → 나중에 아트 추가 여지.
- 배포 확인: firebase deploy 완료, 에셋 7종 전부 HTTP 200, 배포 HTML에 ASSET_VER=15·nodeArt·map_world 반영 확인.

---
## 2026-07-24 저녁 배포 (v16) — 몹 스프라이트 + dragon 카드 + 노드 아이콘 완성
- **dragon 보스 카드 생성** (Flow 3:4, 검은 카본 드래곤+용암 코어) → bcard_dragon.jpg → **보스 카드 9종 전부 완성**.
- **신규 몹 스프라이트 5종 생성** (Flow 1:1, 흰배경 → flood-fill 배경제거 → 투명 PNG):
  - boss_m_frostwolf(서리늑대)·boss_m_salamander(잉걸불도롱뇽)·boss_m_jelly(전기해파리)·boss_m_wasp(산성말벌)·boss_m_drone(강철딱정벌레)
  - → 콤보용 신규 몹들이 이제 전투/노드에서 이모지 대신 아트로 표시됨. (25종 몹 전부 스프라이트 보유)
- **노드 아이콘 세트 완성**: 모닥불(assets/node/rest.png, Flow 생성) 추가 → 시작지점·화학실험실·분리실험실·모닥불 전부 이미지화 (이모지 폴백 유지).
- 배포: ASSET_VER 16, 전 에셋 HTTP 200 확인.

### 배경제거 파이프라인 (재사용)
- Flow에서 "PLAIN SOLID WHITE background" 명시 → 흰배경 생성 → Python PIL+numpy BFS flood-fill(가장자리에서 near-white만 제거, (R,G,B)>236 & max-min<14) → 크리처 내부 밝은 부분 보존. rembg 불필요.

### Flow 이미지 회수 팁 (검증됨)
- 렌더 프리즈/다운로드 안 될 때: DOM의 <img src=media.getMediaUrlRedirect...>를 fetch→blob→a.download 로 직접 회수. 최신순=getBoundingClientRect().top 정렬 상단.

### 남은 것
- (없음, 핵심 아트 공백 해소) 필요 시 잡몹 추가 다양화·분자 카드 확장.

---
## 2026-07-24 밤 — 보스 크기 + 저울 파츠화 + 실험실 1문제 흐름
- **보스 스프라이트 확대**: 잡몹 아님(!B.isMob)일 때 `#arena.bossFight` 클래스 → 미디어쿼리별 .bossImg 상한 상향. 사용자 화면(125%≈1536×720)에서 보스 높이 96px→260px. 슬롯과 겹치지 않게 아레나 여백 내 상한 관리(clamp 200~300, 실측 260). 잡몹은 기존 크기 유지.
- **⚖ 저울 파츠화(SVG)**: balance_scale.png(고정 이미지) 폐기 → SVG 파츠(받침대·기둥·빔·사슬·접시)로 재구성.
  - `#balBeamG` 받침점(170,60) 회전 + `#balLeftG/#balRightG` 수직 이동(빔 끝 따라). `transform-box:view-box`로 뷰박스 좌표계 회전.
  - 무거운 쪽 접시가 실제로 내려가고, 원자 라벨(balPanL/R)도 접시 따라 이동. 균형 시 수평+금빛(.balanced).
  - balUpdate: tilt=clamp((totR-totL)*2.4,±13°), dy=sin*118.
- **실험실 1문제 흐름**: 3문제 일괄 → **방문마다 1문제**(showLab 1개 픽, lab.ch). `BAL_NOTE`에 반응식·설명·combo 여부. 정답 시 labSolved: 균형식 표시 + 학습 설명 + (콤보면)"⚔ 전투에서 콤보로 쓸 수 있다!" + 희귀/귀한원소 +3%p, 34% 확률 도구. state.comboHints 기록. 맵 복귀 시 "🧪 새 반응식 습득".
- 검증: node --check 통과, 로컬 서버+실제 Chrome로 보스 크기·저울 기울기·균형·1문제 정답 피드백 전부 육안 확인. 배포 완료(ASSET_VER 16 유지—신규 에셋 없음, HTML은 no-cache).

---
## 2026-07-24 밤 — 🧬 분자 제작 시스템 (원자 수집 → 분자 카드 비축 → 고난도 콤보)
**문제**: 콤보(반응식)는 분자 여러 개가 필요한데 원자 카드만 있어 슬롯이 부족 → 복잡한 반응식 실현 불가.
**해법**: 플레이어가 모은 원자로 분자 카드를 미리 제작·비축해 전투에서 꺼내 쓰게. 도구 획득으로 게이팅해 난이도 곡선 형성.

- **토큰 인스턴스 id**: 기존 분자 토큰 `@H2O`는 중복 배치 불가였음(콤보엔 `@H2 ➕ @H2` 필요). 토큰을 `@H2|7`(|뒤 고유 id) 형식으로 확장. `tokKey`=키만, `tokId`=id. 하위호환(랜덤 희귀 `@H2O`는 id 없음).
- **신규 도구 「분자 합성기」(molgear)**: `noEquip:true` — 소지만 하면 공방 해금(착용 슬롯 안 먹음). gainTool이 noEquip은 자동착용 스킵, 장비화면에서 제외. **STAGE 2 진입 시 데토스가 확정 지급**(RNG 아님, 모든 플레이어 동일 곡선). victory flow `state.stage++` 직후.
- **분자 공방(craftScreen)**: 맵 허브 버튼(🧬 분자 공방, molgear 소지 시 노출) + **모닥불 휴식 노드에서도 자동 오픈**(회복+제작 거점). 발견한(`state.known`) 반응식만 제작, 원자(`state.inv`) 소비. 제작 시 `state.checkpoint` 갱신(패배 복구 환불 악용 방지).
- **분자 주머니(state.pouch=[{key,id}], pouchMax=5)**: 영구 저장. 전투 손패에 🧬주머니 카드로 렌더, 클릭→`addMolToSlot(id)`로 슬롯 배치(중복 허용). 발사 시 `consumeMolTokens()`가 주머니(|id)/랜덤희귀(rareHand) 구분 소모.
- **세이브**: pouch·pouchMax·molIdSeq 추가.
- **검증(로컬 서버+Chrome)**: 공방에서 H₂×2·O₂×1 제작 → 원자 차감 확인 → 전투 배치 `@H2|1 ➕ @H2|2 ➕ @O2|3` → matchEquationCombo=form_water balanced → 발사 시 계수비 2:1:2 발견 연출 → 보스 120→42(78뎀, 약점) → 주머니 소모·combosFound 등록. 전 과정 정상.

### 다음 확장 여지 (미구현)
- 주머니 상한 확장을 레벨/보상에 연결, 고급 분자(NH₃·CH₄) 제작에 상위 도구 티어, 공방 UI에 "이 분자로 만들 수 있는 콤보" 힌트.

---
## 2026-07-25 오전 — 🎬 시네마틱 컷신 아트 41장 전부 생성·설치 (자율)
사용자 퇴근, "최대한 많은 이미지" 요청 → 보스 9종 컷신 전 패널을 프롤로그 앵커에 맞춰 완성.
- **앵커**: 프롤로그 11장(따뜻한 회화풍 2D·갈색 잉크 외곽선·스토리북·16:9) = 스타일 기준.
- **공통 프롬프트**: STYLE prefix + 캐릭터 일관성(보라 후드 연금술사 / 낡은 토기 항아리에서 피는 푸른 정령 = 단지의 정령). 스크래치패드 cut_all_scenes.md에 전 장면 정리.
- **생성·설치 (assets/cut/cut_<보스>_<pre|post><n>.jpg, 폭1280 q86)**:
  - ignis(pre2·pre3·post1·post2 신규, pre1 기존)=5, frost 5, aquarion 4, acid 4, ferrum 5, golem 4, phantom 4, lime 4, dragon 6 = **보스 41패널**. + 프롤로그 11 = 총 52장.
  - 코드 CUTS 구조가 이미 이 경로에 이모지 폴백으로 연결돼 있어 **코드 수정 0** — 파일만 떨궈 자동 표시.
- dragon 피날레(용을 타고 검은 얼룩이 초록으로 물드는 세계) 포함, 전 보스 pre(도입)~post(정화) 서사 완결.
- 배포·HTTP 200 검증 완료.

### Flow 대량 생성 실전 팁 (이번 세션 학습 — 에셋도구_사용법.md 반영 요망)
- 보스마다 새 프로젝트(16:9 자동 상속). 홈 "새 프로젝트" 버튼은 **카루셀 배너가 클릭을 가로채** 첫 클릭이 빗나감 → navigate 후 wait 2~3s, 클릭, 스샷 확인, 안 열리면 재클릭.
- 다운로드: DOM 16:9 img를 top정렬(최신=맨위) fetch→a.download. **Chrome이 빠른 연속 다운로드를 ~4개로 스로틀** → 5장 이상이면 간격 1200~1400ms, 받은 개수 검증 후 부족분 재수집.
- 최신순 = post(뒤)→pre(앞) 역순. 제출 순서(pre1..postN)의 역이라 매핑 주의.

### 남은 것
- 퀴즈 10문제(사전/사후 평가지) — 아직 대기.
- 세계지도 시나리오 정합성 수정 — 나중.

---
## 2026-07-26 — ⚖ 저울을 브라스 아트 파츠로 교체 (SVG→실제 이미지)
사용자: "저울 이미지 플로우에서 파츠별로 뽑아서 완성하자".
- Flow(1:1)에서 파츠 3종 생성: 받침대(scale_stand), 빔(scale_beam, 중앙 축보스+양끝 고리), 접시+사슬(scale_pan, 좌우 공용). 흰배경→near-white 전역 제거(브라스는 금색이라 보존)+트림 → assets/ui/scale_*.png(투명).
- game.html 저울 SVG를 이미지 파츠로 교체:
  - HTML: balScaleWrap 안 <svg> → <img balStand/balPanImgL/balPanImgR/balBeam>.
  - CSS: balStand(고정 하단중앙) / balBeam(left50% top64 width250, transform translate(-50%,-50%) rotate, origin center) / balPanImg(top58 width82, 좌right4·left4) / .balanced=금빛 drop-shadow.
  - balUpdate: #balBeam rotate(tilt), #balPanImgL/R translateY(∓dy), dy=sin(tilt)*125(반폭125px). 라벨(balPanL/R) 동일 이동.
- 물리 유지: 무거운 쪽 접시 실제 하강, 균형 시 수평+금빛. 로컬 검증 완료(기울기·균형 스샷).
- ASSET_VER 17, 배포·스케일 이미지 200 확인.

### 남은 것 (변동 없음)
- 퀴즈 10문제 · 세계지도 시나리오 정합성.

---
## 2026-07-26 — 📖 마법책 도감 + ⚡ 콤보 가능 알림
### 마법책 도감 (스크롤→페이지 플립)
- 도감을 5페이지 책으로: 🐉지배자·🧪원소·⚗분자 카드 / ⚗반응식 / ⚡콤보. `.magicBook`(금빛 프레임), `.bookPage`(rotateY 플립, pgCurrent/pgLeft/pgRight), ◀▶ 화살표+점 페이저+페이지명, 좌우 방향키. openCodex→showCdxPage(0).
- renderCardDex: #cdxCards 하나 → #cdxBoss/#cdxElem/#cdxMol 3분할.
- **버그픽스**: 도감 보스카드 class `bossCard`가 전투 보스카드 CSS(width:min300)와 충돌 → `bossDex`로 개명(70×94 정상).
- **레이아웃픽스**: 책이 #game 컨테이너(뷰포트 초과) 기준 중앙이라 하단 nav 잘림 → `.codexScreen{position:absolute→fixed}`(뷰포트 기준). 검증 완료.
### ⚡ 콤보 가능 알림 (renderReactionBar)
- ➕로 분자 이어놓아 콤보 성립 시: "⚡ 콤보 가능! <이름> <반응식> — 지금 합성하면 콤보 발동!"(금빛 펄스). 재료는 맞고 계수비만 틀리면 "재료가 맞다! <반응식> — 계수비를 맞추면 콤보!". → 반응식 반복 노출로 학습.
- matchEquationCombo() balanced 여부로 분기. 배포·검증 완료(ASSET_VER 17 유지).

---
## 2026-07-26 (야간 자율세션) — 🗺 세계지도 재제작 + 🎬 시네마틱 연출/분량 + 🧲 자석 분리 + 🐛 체력바 (ASSET_VER 18)
사용자 "세계지도부터 순서대로 진행해 난 이만 퇴근할게" → 대형아트 그룹 순차 진행. 중간에 "여기 몬스터 체력바 잘린거 고쳐"(스샷) 긴급 삽입.

### 🐛 몬스터 체력바 잘림 (긴급, 사용자 지적)
- `.hpwrap` height 13px + `.hpnum` line-height 16px + overflow:hidden → 숫자 위아래 잘림. → 바 19px·숫자 font14/line19·그림자 강화. 라이브 검증("38/38" 깔끔).

### 🗺 대형아트 ① 세계지도 재제작
- Flow(나노바나나2 16:9) **웅장한 회화풍 판타지 지도** 신규 생성(1376×768). 9바이옴 전부: 화산·불타는숲/설산/바다+바다뱀/녹지/철요새/독성늪/광맥광산/달빛폐허/최종화산. `assets/ui/map_world.jpeg`(+dist), ASSET_VER 17→18 캐시버스트.
- `STAGE_NODES` 재배치: 일정 지그재그(단조로움) → **바이옴 위 유기적 뱀꼴 여정**(화산→설산→해안→바다→평원→숲→요새→광맥→최종화산), 교차·중복 없음. 라이브 검증(1~4 정화✓).

### 🎬 대형아트 ② 시네마틱 (연출 강화 + 분량 확대, 새 아트 0장)
- **연출(A)**: 스테이지 진입 **타이틀 카드**("STAGE n · 단원/지배자명/부제" 극적 등장, 2.5s·클릭 스킵), 상단 **진행 점**, **전 패널 켄번즈**(방향4종 교차), 영화적 **비네트+먼지**, 드라마틱 비트 **흰섬광+진동+저역음**(프롤로그 균열·희생, 드래곤 핵융합), 패널마다 사운드 비트. `cutTitleCard/cutBuildDots/cutBeat`, playCut/cutAdvance/cutFinish 갱신.
- **분량(B)**: **나레이션 인터타이틀 카드**(영화 자막식 텍스트 전용, `{narr,emph}`) 도입. cutPanelHTML/cutAdvance **아트 인덱스 분리**(imgN) → narr 삽입해도 기존 페인팅 파일 정렬 보존(검증). 9보스 **11개 나레이션 비트** 추가(스토리·정서·화학 시적 프레이밍, 티칭X). 메모리 "시네마틱 지금 코드만, 정부지원후 영상교체" 부합.

### 🧲 대형아트 ④ 분리 실험실 고도화 (착수)
- 진단: 5종 중 증류만 인터랙티브, 나머지4는 버튼퀴즈("단순·저급"·티칭). → 자석부터 체험형으로.
- **자석 분리 미니게임**: `#magnetScreen` + `openMagnet/magMove/magSuccess/startMagnetPuzzle` + pointer 드래그 바인딩. 자석 드래그로 철가루(회색)만 자성포획·뭉침, 모래(황색) 잔류, 14/14→Fe해금. `showSepLab` method==='magnet' 분기. 라이브 검증(9/14 뭉침→14/14 성공).
- **남은 것**: 여과·크로마토·재결정 3종도 같은 패턴 인터랙티브화 가능(방향 확정). **사용자 확인 후 확장 권장**.

### 남은 대형아트
- 분리실험실 나머지 3종(여과/크로마토/재결정) · 전반 아트 업그레이드.
- (③ 화학자 구출 연출은 이전 세션 완료)
