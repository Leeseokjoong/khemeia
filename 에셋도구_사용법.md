# Flow · 일레븐랩스 조작 매뉴얼 (2026-07-19 실전 검증)

새 세션이 이 문서만 읽고도 오늘 수준으로 에셋을 뽑을 수 있게 정리함.
**좌표를 외우지 말 것.** 레이아웃이 수시로 바뀐다. 매번 스크린샷 → 좌표 확인 → 클릭.

---

## 공통 원칙 (이걸 어겨서 오늘 여러 번 실패함)

1. **한 번에 한 장/한 소리만 요청.** "Generate 2 images"라고 하면 한 이미지에 좌우로 합쳐 그리거나 하나를 누락한다.
2. **입력 후 반드시 스크린샷으로 확인.** 창 크기가 바뀌면 클릭이 빗나가는데, 확인 안 하면 10개를 통째로 날린다(실제로 그랬음).
3. **채택 전 파형/그림을 먼저 검사.** 듣지도 보지도 않고 1번 변형을 채택했다가 두 번 다시 만들었다.
4. 프롬프트는 **영어**가 품질이 훨씬 좋다.

---

## Google Flow (이미지)

### 접속·설정
- https://labs.google/fx/ko/tools/flow — claude-in-chrome 사용 (인앱 브라우저는 구글 로그인 안 됨)
- PRO 구독 중. 이미지 생성은 **0크레딧**.
- `새 프로젝트` → 하단 프롬프트 칩(`Nano Banana 2 · 1x`) 클릭 → **이미지 / 비율 / 장수** 설정
- **비율**: 컷신·배경 16:9, UI 프레임 1:1, **카드 3:4**
- ⚠ 프롬프트 박스가 **동영상 모드로 멋대로 바뀐다**(크레딧 소모!). 매번 칩이 `Nano Banana 2`인지 확인.
- 프로젝트 URL로 직접 들어가면 `Application error` 가 뜬다. **도구 홈에서 프로젝트를 눌러 들어갈 것.**

#### ⚠⚠ 비율 설정은 **두 군데**에 있고 서로 별개다 (2026-07-21에 이것 때문에 3장 날림)
1. `⚙ > 에이전트 설정 > 이미지 생성 기본값` — 여기를 3:4로 저장해도
2. **프롬프트 바의 `Nano Banana 2` 칩** — 실제 생성은 **이쪽 값**을 쓴다

게다가 프롬프트 바의 **`에이전트` 칩이 켜져 있으면** 에이전트가 비율을 제멋대로 고른다.
세로 카드 프롬프트를 주면 3:4가 아니라 **9:16(768×1376)** 을 골라버린다.

**생성 전 매번 이 순서로 확인할 것:**
1. `에이전트` 칩을 **끈다** → 그러면 오른쪽에 `🍌 Nano Banana 2 ▯ 1x` 칩이 나타난다
2. 그 칩을 눌러 **이미지 / 3:4 / 1x** 를 눈으로 확인
3. 결과가 **896×1200** 이면 성공 (`img.naturalWidth` 로 확인)

### ⚠⚠ 프롬프트 입력 — `textarea`가 아니라 **contenteditable DIV** (2026-07-24)
- `document.querySelector('textarea')` 로 값을 넣으면 **화면엔 안 들어간다.** 그 textarea는 숨김(rect 0,0)이고,
  실제 입력창은 **`[contenteditable=true]` DIV**다. JS로 value를 넣어도 React가 인식 못 해 **제출이 무반응**.
  → **반드시 클릭해서 실제 타이핑**(computer type)할 것.
- ⚠ **입력창에서 `Ctrl+A` 금지.** 포커스가 입력창에 없으면 **미디어 그리드가 전체선택**되어
  생성해둔 이미지들이 **프롬프트에 참조 이미지로 첨부**되고 목록에 필터까지 걸린다(2026-07-24 실제로 당함).
  → 지우고 다시 쓰려면 **새 프로젝트를 만드는 게 가장 빠르다.**
- 제출 버튼은 `arrow_forward` 를 포함한 버튼. **좌표가 자주 바뀌니** 매번 DOM으로 rect를 구해서 클릭할 것
  (설정 패널이 열리고 닫히면 바가 40~60px 위아래로 움직인다).

#### ⚠⚠ DOM rect 와 실제 버튼 위치가 어긋난다 (2026-07-24 재확인)
`getBoundingClientRect()` 로 구한 제출 버튼 좌표 `(1044,663)` 를 눌렀는데 **아무 일도 안 일어났다.**
스크린샷을 보니 실제 화살표는 `(1064,676)` 에 있었다 — rect 가 20px 정도 어긋난다.
→ **DOM rect 로 대충 위치를 잡되, 최종 클릭 전에 스크린샷으로 화살표 픽셀 위치를 눈으로 확인할 것.**
제출 후 `이미지 개수가 늘었는지` 확인해서 안 늘었으면 **제출이 안 된 것**이다(생성 중이면 `%` 가 보인다).

#### ⚠⚠ 창 크기가 바뀌면 입력창 클릭이 '필터'로 들어간다 (2026-07-24)
브라우저 창이 `784→735` 로 줄면서 예전 좌표 `(700,682)` 가 **미디어 그리드**를 눌렀고,
이어서 친 프롬프트가 **필터 검색어로 들어가** `동영상 ✕` `업로드됨 ✕` 칩이 생기고
"선택한 항목과 일치하는 결과가 없습니다" 로 **생성해둔 이미지가 전부 사라진 것처럼** 보였다.
(이미지는 무사했다. 칩의 ✕ 를 눌러 필터를 지우면 돌아온다.)
→ 입력창도 **매번 DOM 으로 rect 를 구해** 클릭할 것:
```js
[...document.querySelectorAll('[contenteditable=true]')].map(e=>{const r=e.getBoundingClientRect();
  return {x:Math.round(r.left+r.width/2), y:Math.round(r.top+r.height/2), 글:e.innerText.slice(0,20)};})
```
→ 그리고 **타이핑 직후 `innerText.length` 로 글자 수를 확인**할 것. 900자 프롬프트인데 17자면 빗나간 것이다.
**제출 전에 확인하면 아무것도 잃지 않는다.**

### 프로젝트 분리 (중요)
이미지가 **7~8장**만 쌓여도 렌더러가 얼어 **스크린샷 CDP가 30초 타임아웃**된다(문서에 10장이라 적었으나
실측은 더 빠르다). **4~5장마다 새 프로젝트**를 만들 것.
오늘 쓴 분리: 컷신 / 배경 / UI프레임 / 철사 / 희귀카드 / 영웅카드

**얼었을 때 대처:** 스크린샷을 포기하고 **javascript_tool 로 DOM만 읽는다**(이건 계속 동작한다).
그림 확인은 **받아서 로컬 PIL로 보는 게** 스크린샷보다 훨씬 안정적이다.

### ⚠⚠ 입력창 포커스가 안 잡히는 문제 — 탭 선택 + dpr (2026-07-24, 오래 헤맴)
증상: `computer left_click`(ref든 좌표든) 후에도 `document.activeElement`가 계속 **BODY**.
타이핑이 입력창에 안 들어감(글자수 17=플레이스홀더). JS `.focus()`는 `활성:✔`가 되지만
**`computer type`은 OS 포커스로 입력**하므로 DOM 포커스와 무관 → 여전히 안 들어감.
- **원인 1 — 탭 선택:** `computer` 클릭은 **`selectedTabId`인 탭에만** 먹는다.
  새 탭(`tabs_create_mcp`)을 만들어도 `selectedTabId`는 옛 탭 그대로일 수 있다
  (`tabs_context_mcp`로 확인). 선택 안 된 탭에 클릭하면 전부 BODY로 샌다. **선택된 탭에서 작업할 것.**
- **원인 2 — dpr 1.25 (윈도우 125% 배율):** `document.elementFromPoint(cx,cy)`로는 입력창이 맞는데도
  `computer` 클릭이 빗나가는 프로젝트가 있었다. 스크린샷폭/innerWidth=1.01인데 dpr은 1.25라
  좌표 변환이 어긋나는 듯. **동일 창인데도 프로젝트마다 되고 안 되고가 갈렸다(간헐적).**
- **실전 결과:** 8장 중 4장만 성공(한 프로젝트만 클릭 포커스가 먹음). 나머지 4장은 새 프로젝트를
  아무리 만들어도 BODY로 샜다. **되는 프로젝트를 잡으면 거기서 4~5장 몰아서 뽑고**(freeze 전),
  안 되면 세션/브라우저를 새로 시작하는 게 답. `elementFromPoint`로 입력창이 맞는지 먼저 확인.

### ⚠ "비정상적인 활동이 감지되었습니다" (2026-07-21)
연속으로 빠르게 생성하면 Google이 자동화로 보고 생성을 거부한다. 카드에 `실패` 로 뜬다.
- **`다시 시도` 버튼 한 번이면 통과**했다.
- 이후 **생성 사이에 10초씩 쉬면서** 진행하면 다시 안 났다.
- 감지 여부는 `document.body.innerText.includes('비정상적인 활동')` 으로 확인.

### 다운로드 — canvas 우회가 유일하게 안정적
`fetch()`는 CSP로 막히고, 다운로드 버튼은 zip으로 떨어진다. 아래를 javascript_tool로 실행:

```js
const im=[...document.querySelectorAll('img')].find(i=>i.src.includes('getMediaUrlRedirect'));
const c=document.createElement('canvas');c.width=im.naturalWidth;c.height=im.naturalHeight;
c.getContext('2d').drawImage(im,0,0);
const b=await new Promise(r=>c.toBlob(r,'image/png'));
const a=document.createElement('a');a.href=URL.createObjectURL(b);a.download='파일명.png';
document.body.appendChild(a);a.click();a.remove();
`${im.naturalWidth}x${im.naturalHeight}`
```
- 여러 장이면 `filter(...)` 후 **dedupe + reverse** (그리드는 최신순).
- ⚠ 그리드는 **지연 로딩** — DOM에 최신 몇 장만 있다. 개수를 세어 확인할 것.
- 저장 위치: `C:\Users\leeseokjoong\Downloads`

#### ⚠⚠ 생성 직후 `[0]`을 잡으면 **직전 카드가 잡힌다** (2026-07-19 실제로 당함)
생성이 끝난 것처럼 보여도 그리드 DOM에 새 이미지가 아직 안 붙어 있어서,
`[0]`이 **바로 전에 뽑은 것**을 가리킨다. 파일명만 새 이름이라 눈치채기 어렵다.
→ Na 카드를 받았다고 생각했는데 C 카드가 두 번 저장됐고, **해시가 같아서** 겨우 발견함.

**반드시 이렇게 검증할 것:**
```bash
# 받은 뒤 직전 파일과 해시 비교 — 같으면 아직 안 올라온 것이니 더 기다렸다 다시 받는다
md5sum raw_card/card_*.png | sort   # 중복 해시가 보이면 그 장은 실패
```
또는 다운로드 전에 DOM 이미지 개수가 **생성 전보다 늘었는지** 먼저 확인한다.
`window.__n` 에 이전 개수를 담아두고 비교하면 확실하다.

#### 원소기호는 크롭해서 눈으로 볼 것
`Cl`(소문자 L)이 `CI`(대문자 아이)로 나오면 화학적으로 틀린 걸 가르치게 된다.
세리프체에서 대문자 I는 위아래 모두 가로획, 소문자 l은 좌상단 플래그 + 아래 발.
```python
im=Image.open('card_Cl.png'); w,h=im.size
im.crop((int(w*.05),int(h*.03),int(w*.40),int(h*.22))).resize((480,300)).save('_chk.png')
```
검증 결과: 한 글자(H·O·C)는 물론 **두 글자(Na·Cl)도 정확히 나온다.**

### javascript_tool 반환값이 `{}`로 나올 때
async IIFE의 반환이 자주 유실된다. **결과를 `window.__x`에 담고 다음 호출에서 `JSON.stringify(window.__x)`** 로 꺼낼 것.

---

## 일레븐랩스

### 효과음 (https://elevenlabs.io/app/sound-effects)
- **1개당 2~7크레딧** (길이에 비례). 매우 저렴. 잔여 32,000+
- 하단 프롬프트 박스 → 길이 칩(기본 4.0s)을 **0.7~2.1s로 줄일 것** (타격음은 짧아야 함)
- 한 번 생성에 **변형 4개** 나옴
- 프롬프트 교체: 박스 클릭 → **Ctrl+A** → 타이핑 (triple_click은 자주 실패)
- 다운로드: 항목의 ↓ 아이콘 → **MP3** (파일명에 프롬프트 앞부분이 들어가 구분됨)
- 못 찾겠으면 `기록` 탭의 검색창에 프롬프트 첫 단어를 넣어 필터

### 음악 (https://elevenlabs.io/app/music)
- **900크레딧/분 × 장수** — 효과음보다 훨씬 비싸다. 꼭 필요할 때만.
- 루프곡은 1분이면 충분. **WAV로 받아** 가공 후 MP3로 인코딩.

---

## 후처리 (ffmpeg / PIL) — 검증된 명령

ffmpeg 경로: `/c/Users/leeseokjoong/Downloads/ffmpeg-8.1-essentials_build/ffmpeg-8.1-essentials_build/bin/ffmpeg`

### 효과음: 타격음으로 다듬기
AI 생성물은 2초씩 나와서 클릭음으로 못 쓴다. **0초에 정점, 0.2초에 소멸**이 목표.
```bash
# 1) 정점 위치 탐색
for ms in 0 20 40 60 80 100; do
  $FF -hide_banner -ss $(awk "BEGIN{print $ms/1000}") -i in.mp3 -t 0.02 \
    -af volumedetect -f null - 2>&1 | grep max_volume
done
# 2) 앞부분만 잘라 증폭
$FF -y -i in.mp3 -t 0.22 \
  -af "afade=t=out:st=0.16:d=0.06,loudnorm=I=-11:TP=-0.6:LRA=6" \
  -c:a libmp3lame -b:a 96k -ar 44100 assets/sfx_place.mp3
```
- 일반 착탄음: `-t 1.0~1.2`, `loudnorm I=-16`
- **볼륨 통일 필수** — 안 하면 폭발음만 크고 물소리는 안 들린다.

### 음악: 이음새 없는 루프
꼬리 C초를 머리에 크로스페이드로 겹친다. `bc` 없으니 `$((D-C))` 사용.
```bash
C=4; D=126; B=$((D-C))
$FF -y -t $D -i in.mp3 -filter_complex \
 "[0:a]atrim=0:$C,asetpts=PTS-STARTPTS,afade=t=in:st=0:d=$C[h]; \
  [0:a]atrim=$C:$B,asetpts=PTS-STARTPTS[b]; \
  [0:a]atrim=$B:$D,asetpts=PTS-STARTPTS,afade=t=out:st=0:d=$C[t]; \
  [h][t]amix=inputs=2:normalize=0[s]; [s][b]concat=n=2:v=0:a=1[o]" \
 -map "[o]" -c:a libmp3lame -b:a 96k out.mp3
```
먼저 **페이드아웃 시작점을 찾아 잘라낼 것**(끝 40초를 5초씩 훑어 음량 급락 지점).

### 이미지: 용량
Flow PNG 원본은 **장당 2.5~2.8MB**. 투명도 불필요하면 JPEG로:
```bash
python -c "from PIL import Image; im=Image.open('x.png').convert('RGB');
im.resize((1280, round(im.size[1]*1280/im.size[0])), Image.LANCZOS).save(
'assets/bg_x.jpg','JPEG',quality=80,optimize=True,progressive=True)"
```
→ 장당 ~150KB (13배 감소). 스프라이트만 PNG.

### 이미지: 빛나는 선만 남기기 (철사·글로우)
배경을 지우는 게 아니라 **밝기를 알파로 변환**한다. 검정이 완전 투명해지고 선만 남는다.
```python
for y in range(H):
    for x in range(W):
        r,g,b,a=px[x,y]
        lum=0.299*r+0.587*g+0.114*b
        px[x,y]=(r,g,b,min(255,int(lum*2.6)))
# 흰 배경 잔재는 채도로 제거 (빛나는 선은 채도 높음)
if max(r,g,b)>150 and sat<0.22: alpha *= 0.12
```

---

## 원소 카드 (타로풍) — 2026-07-19 승인된 앵커

**확정 스타일:** 절제된 고전 타로. 크림색 양피지 + **가는 금색 매듭띠(켈트 인터레이스)** + 남색 별밤.
좌상단/우하단(180° 회전) 양피지 방패 안에 **남색 세리프 원소기호**. 트럼프 랭크 배치.
- ✅ 채택: 가늘고 우아한 금선, 절제된 색, **저채도 남색 별밤 배경**
- ❌ 반려: 보석 박힌 화려한 금테 (산만하고 통일성 깨짐) → 프롬프트에 `NO gemstones` 필수
- 비율 **3:4** (896×1200). 1:1로 뽑으면 카드 주위에 흰 여백이 생겨 못 쓴다.

#### ⚠ 아래 프롬프트의 `vine scrollwork` 는 말과 결과가 다르다 (2026-07-21 확인)
프롬프트엔 "넝쿨 세공"이라 써 있지만 **실제 12장이 뽑아낸 건 가는 매듭띠**다. 이 프롬프트를 그대로
쓰면 대체로 매듭띠가 나오지만, 가끔 **잎사귀 달린 진짜 넝쿨 세공**이 나와서 12장과 안 어울린다.
→ 통일성이 중요하면 `interlacing vine scrollwork` 대신
**`a NARROW restrained gold border band of interlacing knotwork`** 로 쓰는 게 결과에 더 가깝다.
- 실패 사례: `CH4` 는 매듭이 아예 없는 **밋밋한 이중선**, `NaOH` 는 **분홍·주황 성운에 고채도** 로 나왔다.
- 배경이 튀는 걸 막으려면 `LOW SATURATION background` 를 넣을 것.

#### ⚠ `edge to edge` 만으로는 부족하다
`filling the entire frame edge to edge` 만 넣으면 **나무 탁자 위에 놓인 카드 사진**으로 나온다(O2에서 겪음).
→ 반드시 이 문장을 넣을 것:
`The card artwork FILLS 100% OF THE IMAGE, edge to edge, with NO table, NO surface and NO background visible behind or around the card.`

```
A vertical tarot card for a fantasy alchemy game, filling the entire frame edge to edge
with no background margin. Aged cream parchment card stock with softly worn edges and
rounded corners. An elegant SLENDER gold filigree border of interlacing vine scrollwork
frames a deep navy starfield center; small round gold medallions of a sun, a crescent moon
and a six-pointed star sit at the midpoints of the border. In the TOP-LEFT corner and again
in the BOTTOM-RIGHT corner rotated 180 degrees, a cream parchment shield panel holds the
chemical element symbol "<기호>" in DEEP BLUE elegant serif lettering, like the rank on a
playing card. At the center, floating inside a faintly glowing circular alchemical diagram
of arcane sigils: <소재>. Softly painted 2D fantasy illustration, luminous, restrained and
classical, navy gold and cream palette. Only the symbol <기호> appears as text,
NO numbers, NO other words, NO watermark, NO gemstones.
```
두 글자 기호는 `capital N then lowercase a` 처럼 **대소문자를 명시**할 것.

| 기호 | 소재(`<소재>`) |
|---|---|
| H | a weightless golden flame-wisp rising, the lightest of all things |
| O | a swirling spiral of luminous pale-blue breath, the living air itself |
| C | a black crystalline diamond lattice, faint rainbow refraction on its facets |
| Na | a small silver-white metal cube touching dark water and bursting into orange flame |
| Cl | a coiling serpent of pale yellow-green toxic vapour, beautiful and deadly |
| Ca | a pale bone-white limestone pillar, chalky and dry |
| Mg | a blinding white star-flare, pure dazzling light |
| Fe | a glowing red-hot iron blade fresh from the forge |
| K | a bloom of violet flame |
| N | a still drift of cold pale-blue mist, the quiet air |
| S | a cluster of sulphur-yellow crystals burning with a pale blue flame |
| Ba | a blooming green flame |

## 분자 카드 (희귀 카드) — 2026-07-21

원소 카드와 **같은 손패에 섞여 나오므로 위 원소 카드 스타일에 맞춰야 한다.** 원소기호 자리에 분자식이 들어간다.
등급색은 아우라로만 표현하고 테두리·배경은 원소 카드와 동일하게 간다.

| 등급 | 아우라(`<등급색>`) | 대상 |
|---|---|---|
| 고급 | `EMERALD GREEN` | H2 O2 CO HCl |
| 희귀 | `SAPPHIRE BLUE` | H2O CO2 CH4 H2O2 NH3 |
| 영웅 | `AMETHYST VIOLET` | NaOH Ca(OH)2 NaCl CaCO3 NaNO3 Na2SO4 CaCl2 MgCl2 KCl |
| 전설 | `RADIANT GOLD` | H2SO4 BaSO4 |

### ⚠ 소재가 겹치면 손패에서 구별이 안 된다
염(salt) 계열은 전부 "흰 결정"이라 그대로 뽑으면 똑같이 나온다. 실제로 이렇게 겹쳤다:
- `NaNO3` ↔ `Na2SO4` — 둘 다 흰 결정 뭉치
- `CaCO3` ↔ `Ca(OH)2` — 둘 다 유리컵 속 흰 액체

→ **소재를 일부러 갈라놓을 것.** 결정 / 불똥 / 돌덩이 / 유리컵 / 가루처럼 **형태 자체를 다르게** 준다.
손패 카드는 82×110px 이고 겹쳐 들면 **왼쪽 36px 띠만 보인다.** 거기서 구별돼야 한다.

### 대소문자 명시 (필수)
소문자가 든 분자식은 반드시 풀어서 적는다. 이렇게 하면 15장 전부 정확히 나왔다.
```
the formula is spelled capital H, then capital C, then a LOWERCASE letter l
(a lowercase L, never a capital I)
```
- 대상: `HCl` `NaCl` `CaCl2` `MgCl2` `KCl` `NaOH` `CaCO3` `NaNO3` `Na2SO4` `BaSO4`
- `Ca(OH)2` 처럼 괄호가 있어도 `an opening bracket ... a closing bracket, then the digit 2` 로 풀면 아래첨자까지 정확히 나온다.

### 후처리
```bash
python tools/card_trim.py rare    # raw_rare/rare_*.png → assets/card/rare_*.jpg (폭 600, q86)
```
여백이 장마다 **0~35px로 제각각**이라 고정값 크롭 금지 — 실측 트림이 필요하다(`rare` 인자 모드가 그 일을 한다).

### 통일성 검사 — 한 판에 놓고 볼 것
한 장씩 보면 다 괜찮아 보인다. **대조 시트로 모아 봐야** 튀는 게 보인다.
```python
from PIL import Image
W,H=200,268
sheet=Image.new('RGB',(W*5+4*6, H*3+2*6),(18,14,28))
for i,k in enumerate(keys):
    sheet.paste(Image.open('raw_rare/rare_%s.png'%k).convert('RGB').resize((W,H)), ((i%5)*(W+6),(i//5)*(H+6)))
```
**원소 카드 12장 시트도 같이 만들어 나란히 비교할 것** — 맞춰야 할 기준은 그쪽이다.

## 전투 배경 9장 — 대각선 구도 (2026-07-24 완성)

보스가 **우상단**, 플레이어가 **좌하단**에 서는 대각선 전투 구도라 배경도 거기 맞춰야 한다.
구버전(7/19)은 **좌우 대칭 액자형 + 중앙이 텅 빈** 구도라 캐릭터와 정면 충돌했다.

**승인된 문법 (9장 공통):**
1. **넓은 지면 플랫폼이 화면 아래 2/3** 를 차지하고 **좌하 → 우상 대각선**으로 이어진다
2. 배경 요소는 **상단과 좌우 가장자리에만**. 중앙은 비우지 말고 **지면으로 채운다**
3. 굵은 검은 외곽선 · 높은 채도 · 셀셰이딩 2D
4. 스테이지마다 색이 확실히 달라야 한다 (한 판에 모아 놓고 확인)

```
COMMON = "Stylized 2D game battle background, side-scrolling fighting stage, bold black ink
outlines, richly saturated colors, painted cel-shaded fantasy illustration. A WIDE OPEN FLAT
GROUND PLATFORM occupies the LOWER TWO THIRDS of the frame and sweeps diagonally from the
LOWER-LEFT toward the UPPER-RIGHT, leaving clear uncluttered standing room. All scenery
elements sit ONLY along the TOP EDGE and the FAR LEFT and FAR RIGHT margins. The CENTER of
the image is open ground, NOT empty sky. Scene: <장면>. Palette: <색>. Deep perspective,
dramatic lighting. NO characters, NO creatures, NO people, NO text, NO UI."
```
⚠ `NO creatures` 만으로 부족할 때가 있어 해당 보스 이름도 넣는다 (`NO dragon`, `NO ghosts`, `NO statues`).

| 키 | 장면 | 색 |
|---|---|---|
| ignis | 불타는 숲, 갈라진 땅 | 붉은 주황 |
| frost | 얼음 동굴, 얼어붙은 호수 | 청록 얼음 |
| aquarion | 심해 바닥, 빛기둥 | 짙은 청록 |
| acid | 산성 늪 동굴 | 형광 연두 |
| ferrum | 제철소 목재 플랫폼 | 주황 갈색 |
| golem | 폐광 갱도 — 구리광맥, 목재 지지대, 랜턴 | 구리 갈색 + 청록 녹청 |
| phantom | 달빛 폐허 신전, 은빛 안개 | 은백 + 라벤더 |
| lime | 석회 동굴 — 종유석, 천장 빛줄기 | 뼈흰색 + 호박 금색 |
| dragon | 지구 내핵 — 용암 균열 + 거대 다이아몬드 | 용암 주황 + 흑요석 + 보라 |

후처리: `1376x768 PNG → 폭 1280 JPEG q80` (장당 ~180KB)

## 화학자 초상 9명 — 1:1 반신 (2026-07-24 완성)

구출되는 실존 화학자 9명. **아이콘(작게 원형)과 구출 대화창(크게)을 한 파일**로 커버 → 1:1 반신 초상.
`assets/chem/chem_<보스키>.jpg` (512px, q85). 원본 PNG는 `assets/_source/chem/`.

**앵커 = 멘델레예프(ignis)** — 64px 아이콘에서도 얼굴이 읽히고 배경 9장과 스타일이 통일됨.
**공통 골격(그대로 재사용):**
```
Stylized 2D game character portrait, bust shot from the chest up, centered, head in the
upper middle, facing the viewer. Bold black ink outlines, cel-shaded painted fantasy
illustration, richly saturated colors. Warm rim light on the shoulders, simple dark
vignette background so the face reads clearly even at small icon size.
<성격 한 줄>. Character: <시대·국적·외모·복장·소품 한 줄>. Palette: <색3개>.
NO text, NO letters, NO watermark, NO frame, NO border.
```
★ **소품으로 구분** — 두루마리(멘델)·저울(라부아지에)·전지(데이비/볼타)·리트머스(아레니우스)·
불꽃고리(셸레)·식물병(프리스틀리)·석회수잔(블랙)·발광바이알(퀴리). 얼굴만으론 작을 때 구별이 안 된다.
- 비율 **1:1**(crop_square, 1024×1024). 프롬프트가 ~730자면 정상.

## 게임 적용 시 주의

- **캐시**: 에셋 URL에 `?v=${ASSET_VER}` 없으면 파일을 바꿔도 브라우저가 옛것을 쓴다. 교체 시 `ASSET_VER` 올릴 것 (현재 `'5'`).
- **9-슬라이스**: 프레임 교체 시 `border-image ... <slice> / <width>` 의 slice를 **실측**해야 한다.
  성긴 무늬면 `round` 말고 **`stretch`** (round는 반복해서 다시 빽빽해짐).
- 배포: `cp game.html dist/index.html && cp assets/* dist/assets/ && firebase deploy --only hosting`
- 배포 후 `curl -o /dev/null -w "%{http_code}"` 로 파일 200 확인 (복사 누락이 잦음).
